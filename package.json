{
  "name": "my-lunch-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "flutter build web --release",
    "dev": "flutter run -d web",
    "clean": "flutter clean",
    "get": "flutter pub get"
  },
  "dependencies": {
    "node-fetch": "^2.6.1",
    "@netlify/functions": "^2.0.0"
  },
  "devDependencies": {
    "@types/node": "^14.0.0",
    "@types/node-fetch": "^2.5.7"
  }
} 